# P4_trasnferLearning
Models are trained on the AntEye dataset for the Place Learning task. Model heads changed. models further trained on Direction Learning task. Does this improve the models average accuracy on the direction learning task compared to models only trained on this task.
